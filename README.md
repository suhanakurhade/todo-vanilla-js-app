# To-Do List Web App (Frontend only) — Vanilla JavaScript

## Overview
Simple, responsive To-Do list web app built with **vanilla JavaScript**.  
Features:
- Add tasks (Enter or Add button)
- Toggle complete by clicking task
- Delete tasks (trash button)
- Filter: All / Active / Completed
- Clear all completed
- Instant UI updates (no reload)

## How to run locally
1. Clone repository:
   ```bash
   git clone <your-repo-url>
   cd your-repo
